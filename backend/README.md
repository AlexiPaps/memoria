# Backend — NestJS + GraphQL + Drizzle + pgvector

- GraphQL API
- File upload with GPT-4o extraction
- Real OpenAI embeddings
- Semantic search via pgvector cosine distance
- Drizzle ORM + PostgreSQL